# 仅忽略【根目录】下以下划线 _ 打头的文件和文件夹。
# 前导 / 把模式锚定到根目录:嵌套在普通文件夹里的 _ 打头文件不受影响
# (例如 src/_internal.py 仍会被 git 追踪)。
/_*

# 项目规划/协作文档,不纳入版本控制
CLAUDE.md

# Python 构建/缓存产物
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
